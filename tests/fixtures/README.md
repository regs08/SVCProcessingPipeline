# `tests/fixtures/` - Test Fixtures

This directory contains compact synthetic fixtures used by the pytest suite.
Fixtures must not contain private field metadata or real GPS/location-bearing
raw spectra.
