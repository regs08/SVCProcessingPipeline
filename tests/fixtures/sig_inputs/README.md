# Synthetic `.sig` Inputs

The `.sig` files in this directory are hand-written synthetic snippets for unit
tests. They are not field data and are exempted from the repository-wide `*.sig`
ignore rule.
